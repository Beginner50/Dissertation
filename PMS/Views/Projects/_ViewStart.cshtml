@using System.Runtime.CompilerServices
@* 
    _ViewStart contains code which is executed before rendering any view within its scope.
    In that case, this code is executed before rendering any view within the Home directory.
*@

@{
    Layout = "_Layout";
}